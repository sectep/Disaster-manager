package disastermanager.main;

// А class, that manages the console interface.
public class Main {
    public static void main(String[] args) {
        ConsoleInterface application = new ConsoleInterface(); // Initialize console interface.

        application.launch(); // Launch the interface.
    }
}
